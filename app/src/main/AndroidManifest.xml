<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.senac.sp.l13.boaviagem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login_Activity">

            <!-- android:theme="@style/AppTheme.NoActionBar" -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Principal_Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Gasto_Activity" />
        <activity android:name=".Viagem_Activity" />
        <activity android:name=".Lista_Gasto_Activity" />
        <activity android:name=".Lista_Viagem_Activity" />
        <activity android:name=".Configuracoes_Activity" />
        <activity android:name=".Borda_Activity"></activity>
    </application>

</manifest>